# Descriptor for OpenAI-compatible agent runners (Codex, ChatGPT agent mode).
# Claude Code ignores this file and reads SKILL.md directly.
name: init
description: Prepare a project's AI operating context by writing AGENTS.md and registering the plan/slice/exec/review/check/fix workflow.
instructions: ../SKILL.md
model: gpt-5
tools: [shell, read_file, write_file, apply_patch, search]
